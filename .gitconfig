[user]
	email = stevenlu443@gmail.com
[core]
	editor = vim
	whitespace = cr-at-eol
	excludesfile = ~/.gitignore
	autocrlf = input
[color]
	ui = true
[alias]
	lg = log --graph --date-order --pretty=format:'%Cred%h%Creset -%C(bold magenta)%d%Creset %s %Cgreen%ci %C(yellow)(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
	s = "!f() { git status --short; ignored=$(git ignored); [ -n \"$ignored\" ] && echo \"$ignored\"; };f"
	unadd = reset HEAD
	d = difftool
	ignore = update-index --skip-worktree
	unignore = update-index --no-skip-worktree
	ignored = !git ls-files -v | grep "^S"
	status-with-ignored = "!f() { git status; ignored=$(git ignored | cut -d ' ' -f 2 | sed -e s/^/[31m/ -e s/$/[m/); [ -n \"$ignored\" ] && echo \"git skip-worktree (ignored):\n$ignored\"; };f"
	diff-with-ignored = "!f() { git diff --no-ext-diff; ignored=$(git ignored | cut -d ' ' -f 2); [ -n \"$ignored\" ] && echo \"git skip-worktree (ignored):\" && for var in $ignored; do echo [31m$var[m; git show HEAD:$var | diff - $var; done };f"
	diff-with-ignored-ext = "!f() { git diff --ext-diff; ignored=$(git ignored | cut -d ' ' -f 2); [ -n \"$ignored\" ] && echo \"git skip-worktree (ignored):\" && for var in $ignored; do echo [31m$var[m; git show HEAD:$var | sift - $var; done };f"

	commit-push = "!f() { git commit -am \"$*\" && git push; };f"
[difftool]
	prompt = false
[diff]
	external = sift
	# tool = pydifflib
	tool = opendiff
[pager]
	log = less
	show = less
	# diff = less
[color]
	branch = auto
	#diff = always
	status = always
[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green
[color "diff"]
	meta = blue
	frag = magenta
	old = red
	new = green
	whitespace = blue reverse
[color "status"]
	added = yellow
	changed = green
	untracked = cyan
[rerere]
	enabled = true
[merge]
	stat = true
[difftool "sourcetree"]
	cmd = opendiff \"$LOCAL\" \"$REMOTE\"
	path = 
[mergetool "sourcetree"]
	cmd = /Applications/SourceTree.app/Contents/Resources/opendiff-w.sh \"$LOCAL\" \"$REMOTE\" -ancestor \"$BASE\" -merge \"$MERGED\"
	trustExitCode = true
[push]
	default = simple
