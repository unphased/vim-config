id = "local.pane-load"
name = "Pane Load"
version = "0.1.0"
min_herdr_version = "0.9.0"
description = "macOS pane CPU, memory, and process-chain metadata"
platforms = ["macos"]

[[startup]]
command = ["cargo", "run", "--quiet", "--release", "--", "start"]
platforms = ["macos"]

[[actions]]
id = "start"
title = "Start pane load sampler"
contexts = ["global"]
platforms = ["macos"]
command = ["cargo", "run", "--quiet", "--release", "--", "start"]

[[actions]]
id = "stop"
title = "Stop pane load sampler"
contexts = ["global"]
platforms = ["macos"]
command = ["cargo", "run", "--quiet", "--release", "--", "stop"]
