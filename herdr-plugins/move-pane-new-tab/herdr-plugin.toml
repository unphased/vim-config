id = "local.move-pane-new-tab"
name = "Separate Pane"
version = "0.2.0"
min_herdr_version = "0.9.0"
description = "Move a pane to a new tab, or a new workspace when it is the tab's only pane"
platforms = ["linux", "macos", "windows"]

[[actions]]
id = "move-pane-new-tab"
title = "Separate pane"
description = "Move this pane to a new tab, or a new workspace when it is the tab's only pane"
contexts = ["pane"]
platforms = ["linux", "macos", "windows"]
command = ["./move-pane-new-tab.cmd"]
